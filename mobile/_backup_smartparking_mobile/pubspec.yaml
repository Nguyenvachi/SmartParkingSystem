name: smartparking_mobile

description: SmartParking mobile app (Auth, Dashboard, Booking, Profile)

publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2

flutter:
  uses-material-design: true
